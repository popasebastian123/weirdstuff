#include <bits/stdc++.h>

int a[11] , i , n , k;

int main()
{
 cin>>n;
 for(i = 1;  i < = n ; i ++ )
   cin>> a[i];
  for(i =1 ; i < = n ; i ++ )
  if(a[i] % 2 == 0)
     k++;
     
  for(i = 1 ; i < = n ; i ++ )
    cout<< a[i] <<" " << k;
    return 0;
}
